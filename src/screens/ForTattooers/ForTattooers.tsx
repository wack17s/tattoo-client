import { Body } from '../../components/Body'
import { HeaderMenuButton } from '../../components/Header'

export const ForTattooers = () => {
  return (
    <Body selectedButton={HeaderMenuButton.FOR_TATTOOERS}>
    </Body>
  )
}
