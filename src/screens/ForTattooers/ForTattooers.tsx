import { Body } from '../../components/Body'
import { Header, HeaderMenuButton } from '../../components/Header'

export const ForTattooers = () => {
  return (
    <Body>
      <Header selectedButton={HeaderMenuButton.FOR_TATTOOERS} />
    </Body>
  )
}
